export default function Dashboard () {
    return (
        <main className="w-full h-full flex flex-col sm:flex-row ">
            <section className="w-full relative">
                running tailwind css
            </section>

            <section className="w-full">
                running tailwind css
            </section>
        </main>
    )
}